pipeline {
  agent any
  triggers {
        cron('*/2 * * * *')
    }
  environment {
        Num1 = 9
        Num2=80
    }
  stages {
    stage('version') {
      steps {
        sh 'python --version'
      }
    }
    stage('hello') {
      steps {
        sh 'python loopExec.py'
      }
    }
  }
}
